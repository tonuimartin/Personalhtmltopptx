Place .ttf font files here (e.g. Inter.ttf, Roboto.ttf).

- Puppeteer injects these via @font-face during conversion (offline).
- Optional: install the same fonts on Windows so editable mode text matches.
- "Export bundled fonts sidecar" copies these files next to the output .pptx.
